interface:
  display_name: "Heimdall PR Review"
  short_description: "Review Heimdall diffs for bugs, risks, and missing tests."
  default_prompt: "Use $heimdall-pr-review to review this Heimdall branch for correctness, regressions, and missing tests."

policy:
  allow_implicit_invocation: true
